name: collector-dotnet-nuget
type: collector

cases:
- name: Basic test
  dockerfile: Dockerfile.collector
  repo_contents: test/repo_base
  dependencies_json: test/dependencies.json
  user_config:
    path: /repo_base.sln
